% templates/preamble.tex
\documentclass[letterpaper, 12pt, final]{article}
% Page Layout configurations.
\usepackage[margin=2.5cm]{geometry} % Adjust page margins if needed

% Fonts and text settings
\usepackage[T1]{fontenc} % sets the font encoding of the document to T1, improving various glyph representations
\usepackage[default]{sourceserifpro} % set Document font
\usepackage{parskip} % for better paragraph separation
\usepackage{enumitem} % for better lists
\usepackage[document]{ragged2e}

% Images and floats configurations.
\usepackage{graphicx} % For including images
\usepackage{subcaption}  % for supporting sub-figures and sub-tables within a single figure or table environment
\captionsetup[figure]{font=small,labelfont=bf}
\usepackage{float} % Improved floating for Tables and figures

% Additional features and utilities.
\usepackage[obeyDraft]{todonotes}
\setlength{\marginparwidth}{2cm}
\usepackage{draftwatermark}
\usepackage{authblk} % for better author block
\usepackage{xcolor}    % for colors

% Date formats
\usepackage[showdow, en-US]{datetime2}

% Hyperlinks
\usepackage[
    colorlinks=true,
    linkcolor=blue,
    urlcolor=blue,
    citecolor=blue,
]{hyperref} % for hyperlinks

% Custom macros used in this example document
\newcommand{\doclink}[2]{\href{#1}{#2}\footnote{\url{#1}}}

\newcommand{\module}[1]{\href{https://drupal.org/project/#1}{drupal/{#1}}}
\newcommand{\github}[1]{\href{https://github.com/#1}{#1}}
